generator client {
  provider     = "prisma-client"
  output       = "../generated"
  moduleFormat = "esm"
}

datasource db {
  provider = "sqlite"
  url      = env("SQLITE_PATH")
  // provider = "postgresql"
  // url      = env("POSTGRES_URL")
}
